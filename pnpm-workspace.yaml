packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 reads overrides from the workspace file, not package.json.
overrides:
  "sharp@<0.35.0": "^0.35.0"

# Build scripts required by native/runtime dependencies.
onlyBuiltDependencies:
  - "@prisma/engines"
  - "argon2"
  - "esbuild"
  - "prisma"
